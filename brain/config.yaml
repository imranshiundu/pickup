model:
  provider: anthropic          # or: ollama, openai, groq, lmstudio
  model_name: claude-3-haiku-20240307
  fallback: ollama/llama3.2    # if API is unreachable, use local

ollama:
  base_url: http://localhost:11434
  model: llama3.2              # or mistral, phi3, gemma2 — anything
